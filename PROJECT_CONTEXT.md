# 道案内エディタ（wayfinding-tool）経緯まとめ

このドキュメントは、Claude（claude.ai）とのチャットでプロトタイプを作った経緯と、その後の改修内容をまとめたものです。ここから `michi-annai-editor.html` を改善していく際の背景資料として使ってください。

## 1. 作りたいもの

徒歩で撮影した「道案内用の動画」をアップロードすると、自動でそれっぽく編集してくれるツール。

- 止まっている・景色が変わらない区間を早送り
- 「道なりに進みます」「右に曲がります」「左に曲がります」「到着しました」といったテロップを自動で挿入
- 理想は「アップロードするだけ」で、ある程度見られる編集済み動画がポン出しできること
- 家族・知人に配布する想定（自分専用ではないが、公開サービスでもない）
- **使う人はパソコンに詳しくない**。用語や設定値を説明なしに見せない、間違っても直せる、という点を優先する

## 2. 決まった要件

壁打ちの中で以下を確認・決定した。

| 項目 | 決定内容 |
|---|---|
| 動作環境 | Webアプリ（ブラウザで完結） |
| 対応OS | Windows / Mac 両対応 |
| 配布方法 | 家族・知人にファイルまたはURLを共有 |
| 想定動画 | 徒歩でスマホ撮影、数分程度の短尺 |
| テロップの中身 | 地名・目印などを手動入力（音声認識の自動文字起こしは使わない） |
| テロップの操作感 | 位置を自由に指定できればよい（実装方法は一任） |
| 早送りの基準 | 同じ景色・停止区間を自動検出 |
| 実装 | Claudeに一括で作ってもらう |

### Webアプリを選んだ理由
- デスクトップアプリでWin/Mac両対応にすると、Electron等でのビルド・署名・配布の手間が大きい（特にMacは「開発元不明」で弾かれやすく、知人配布のハードルになる）
- Webならブラウザさえあればよく、配布はファイルまたはURLを渡すだけ
- 動画は数分程度の短尺なので、**サーバーにアップロードせずブラウザ内で処理を完結**できる設計にした
  - 動画（家周辺の映像など）がどこにも送信されないのでプライバシー面でも安心
  - サーバー費用・保管コストがかからない
  - 家族配布という小規模な用途にちょうど良い規模感

### 変えてはいけない制約
以降の改修でも、次の点は維持すること。

- サーバー通信をしない（解析も書き出しもすべてブラウザ内）
- 単一のHTMLファイルで完結する（ビルド手順なし、追加の外部ライブラリなし。外部読み込みはGoogle Fontsのみ）
- 数分の動画をタブが固まらずに処理できる（進捗表示つきの非同期処理）
- ユーザーが区間の早送りON/OFFを切り替えられ、テロップを編集・ドラッグできる

## 3. 技術的な設計

### 全体構成
単一のHTMLファイル（`michi-annai-editor.html`）に HTML/CSS/JS をすべて内包。外部ライブラリはGoogle Fontsの読み込みのみで、ffmpeg.wasmなどの重い外部依存は使っていない（後述の理由による）。

### 処理の流れ
1. `<input type="file">` で動画を読み込み、`URL.createObjectURL` で `<video>` にセット
2. 動画のメタデータ読み込み後、モーション解析を実行（進捗バー・中止ボタンつき）
3. 解析結果から「停止」「直進（うち単調な長い直進）」「曲がり角」を自動分類し、2段階の早送り区間とテロップを自動生成
4. プレビュー画面で確認・手動調整（区間ごとの早送りON/OFF、曲がり角の向きの修正・追加・削除、テロップの位置・テキスト・時間の編集）
5. 「書き出す」ボタンで実際の動画ファイル（webm/mp4）として書き出し・ダウンロード

### モーション解析（2回目の改修で全面的に作り直した部分）

初版は「0.4秒ごとに `currentTime` をシークし、120×68に縮小したグレースケール画像の**全画面平均輝度差**を動きの量とする」という方式だったが、精度が低いという指摘を受けて全面的に作り直した。

初版の主な問題点（作り直した理由）:

- **平均輝度差は自動露出に弱い**。日向から日陰に入るだけで画面全体の輝度が変わり、止まっていても「動いている」と判定された
- **閾値がスライダーの分位点だった**ため、たとえば「感度30」なら映像の内容に関係なく**常に全体の30%が停止と判定される**。まったく立ち止まらない動画でも3割が早送りになり、逆にずっと立ち止まっている動画では取りこぼした
- **曲がり角の判定に使っていたブロックマッチングの探索範囲が±6pxしかなかった**。0.4秒間隔だと曲がり角では景色が数十px流れるので、真の一致位置が探索範囲の外にあり、範囲内のノイズを拾っていた（＝ほぼ当てずっぽう）
- **同点処理にバイアスがあった**。空や白い壁のような模様のない場所では全探索位置のSADがほぼ同じになり、最初に走査した位置（探索範囲の隅）が常に勝つため、**特定方向へ偏った偽の動き**が出ていた
- **測定点が左右2点だけ**だったため、通行人や車がその1点を隠すだけで判定が壊れた
- 模様の有無（テクスチャ）や一致の確からしさ（信頼度）を一切見ていなかった
- サンプル間隔・最小区間長がすべて固定で、動画の長さやフレームレートに適応しなかった

現在の方式:

**(a) フレームの取り出し**
`video.requestVideoFrameCallback` が使えるブラウザ（Chrome / Edge / Safari / 最近のFirefox）では、**動画を早送りで1回再生しながら**フレームを受け取る。数百回シークするより速く、フレームが飛ばず、タブも固まらない。使えない場合は従来どおりのシーク方式に自動で切り替わる（タイムアウト保護つき）。
サンプル間隔は約0.15秒を狙う。長い動画では自動的に広げるが、曲がり角を取り逃さないよう0.30秒までに抑えている。

**再生速度は固定ではなく自動で落とす**（3回目の改修で追加）。ここは実機のサンプル動画（1080×1920・5分26秒）で分かった重要な点で、**実際のサンプル間隔を決めるのは設定値ではなくブラウザのデコード速度**だった。大きな縦動画を6倍速で流すと、実際に表示されるフレームは動画時間で0.23秒に1枚程度（混んだ場面では1.1秒空くこともある）にしかならず、狙いの0.15秒の倍以上になっていた。曲がり角では画面が一気に流れるので、この間隔の開きがそのまま「1サンプルあたりの移動量が測れる範囲を超える」ことに直結する。そこで**実際のフレーム間隔を監視し、開きすぎていれば再生速度を段階的に落とす**（6倍→最低2.5倍）ようにした。サンプル動画では約2.9倍に落ち着き、間隔の中央値が0.23秒→0.17秒、最大が1.1秒→0.5秒に改善した。代償として解析時間は動画長の約1/6から約1/3に伸びる。

**(b) 画像の前処理**
フレームを長辺160pxのグレースケールに縮小し、1/2・1/4の縮小版を作る（画像ピラミッド）。
照合に使う画像は**ハイパスフィルタ**（元画像からぼかし画像を引いたもの）を通す。これによって画面全体の明るさ・色味の変化が打ち消され、**自動露出の変化が「動き」として検出されなくなった**。

**(c) 動きの測定（ブロックマッチング）**
画面上の15か所（横5×縦3、空や地面ばかりにならない高さ）で、前フレームからどれだけ移動したかを測る。

- **粗密探索**：1/4サイズで±10px探索 → 1/2サイズで±1補正 → 原寸で±1補正。実効的な探索範囲は**±43px**（初版の±6pxの7倍）
- **足りないときは範囲を広げる**（3回目の改修で追加）。上記の±43pxでも、実機のサンプル動画の曲がり角では足りなかった（後述）。そこで**そのサンプルが「速い動き」に見えるときだけ、探索範囲を±91pxに広げて測り直す**。速い動きの判定は「一致位置が探索窓の縁に張り付いている」「模様のあるブロックの半分以上が判別不能で落ちた」のどちらか。まっすぐ歩いている間はまず発動しないので、追加の計算コストは必要なサンプルにしか掛からない（サンプル動画1761サンプル中132サンプル、7.5%）
- **サブピクセル補間**：SADの最小点とその両隣に放物線を当てはめ、**小数点以下の精度**で移動量を求める。整数pxのままでは「手ぶれで止まっている」と「ゆっくり歩いている」が区別できなかった
- **テクスチャ判定**：模様の乏しいブロック（空、白い壁）は最初から使わない
- **信頼度判定**：一致の谷が浅い（どこでも同じくらい合う）ブロックは捨てる
- **多重解の棄却**：フェンス・タイル・横断歩道のような繰り返し模様では、離れた位置でも同じくらい一致してしまう。そういうブロックは「判別不能」として**捨てる**（でたらめな値を投票させない）
- **動きベクトルのペナルティ**：ほぼ同点なら移動量の小さいほうを選ぶ（動画コーデックと同じ正則化）
- 最終的な値は15か所の**中央値**。通行人や車が数か所を隠しても結果が動かない

**(d) 停止／歩行の判定**
- 直前フレームではなく**約0.45秒前のフレーム**と比べる。手ぶれは往復するので蓄積しないが、歩行による移動は蓄積するため、両者の差がはっきり出る
- 移動量は**「1秒あたり何度ぶん景色が動いたか」（度/秒）**で表す（3回目の改修で変更）。以前は「1秒あたり画面何個ぶん」だったが、これは**縦動画と横動画で意味が変わってしまう**。同じ動きでも、水平画角の狭い縦動画では画面幅に占める割合が1.6倍大きく見えるため、同じ数値の閾値が横動画には甘く縦動画には厳しく効いていた。度/秒にすれば向きに依存しない
- 閾値は**絶対値（既定 1.0度/秒）**を基準にする。実測では、手に持って立ち止まっているカメラは1度/秒をかなり下回り、普通に歩いていると1.5〜5.5度/秒なので、この値で分離できる
- **判別分析法（大津の二値化・対数スケール）は「閾値を下げる方向にしか」使わない**（3回目の改修で修正）。動画によっては停止中でも手ぶれが大きいことがあるので下げる余地は残すが、上げることは許さない
- ここは3回目の改修で作り直した部分で、**2回目の実装には初版と同じ致命的な欠陥が残っていた**。当時は閾値の上限を「絶対値の上限」と「その動画自身の代表的な動き量（p75）の半分」の小さいほうにしていた。一見「動画に適応する賢い方法」に見えるが、**後者は分位点由来なので、どんな動画でも必ず一定の割合が閾値を下回る**。これは初版の感度スライダーとまったく同じ欠陥だった。実際、一度も立ち止まっていない実機サンプルでは、動き量の分布が**単峰**（山が1つだけ）なのに閾値がその山の左斜面を切ってしまい、**存在しない停止を33箇所**でっちあげていた
- **区間の中央値で最終確認する**（`dropLatchedStops`、3回目の改修で追加）。ヒステリシスには「一度入ると抜けにくい」という副作用があり、たった1サンプルが閾値を下回っただけで停止状態に入り、抜けるには高いほうの閾値を超える必要があるため、普通に歩いているだけなのに数秒間「停止」が続いてしまう。実際、でっちあげられた33箇所の停止は**その大半が区間の中央値では閾値を上回っていた**（＝1サンプルの落ち込みで固まっただけ）。そこで、区間ができたあとに**その区間の動き量の中央値が閾値を下回っているか**を確認し、下回らないものは歩行に戻す。本当に止まっていれば区間の大半は静かなはずなので、この確認は素直に効く
- ヒステリシスの幅も1.6倍から1.25倍に狭めた
- 値には中央値フィルタ（3点）をかけ、鳥が横切った・1フレームだけデコードが乱れた、といった単発のノイズを消す
- 短すぎる区間は捨てずに隣とマージする（初版は捨てていたため、タイムライン上に「どちらでもない穴」ができていた）
- 立ち止まりがほぼ無い動画では、その旨と「早送りを自動では入れていない／手動で入れられる」ことを画面に表示する

**(e) 曲がり角の判定**
初版の「左右2ブロックのdxが同符号なら曲がり」という判定をやめ、**光学的な流れを2成分に分解**する方式にした。

各ブロックの水平移動量 dx を、画面上の横位置 u（中央が0、左端-0.5、右端+0.5）に対して

```
dx(u) ≒ A + B × u
```

と表す。

- **A = パン成分**（カメラが左右に振れた量）
- **B = 広がり成分**（前に進むと景色が左右に広がっていく量）

BはL/R両側の中央値の差から、Aはそこからの残差の中央値から求める（どちらも中央値なので外れ値に強い）。
なおBは「左右のブロック群がどれだけ離れているか」で割って求めるため、曲がり角の途中でブロックが大量に脱落して左右の重心が近づくと、**とんでもない値（実測で1サンプルあたり108px）に発散してAを汚染する**ことがあった。そこで左右の重心が十分離れているときしかBを信用せず、さらにBの大きさを実際に観測された移動量と同程度までに制限している（3回目の改修）。

Aを時間方向に積分し、**「何度向きが変わったか」に換算**する。この値が閾値（既定35度）を超えたら曲がり角とみなす。

**画角は固定値ではなく、動画の縦横比から計算する**（3回目の改修で修正）。これは実機テストで見つかった重要な誤りで、以前は横位置の換算に一律「水平画角67度」を使っていた。67度が妥当なのは16:9の横長動画のときだけで、**スマホを縦に構えて撮った9:16の動画では水平画角は約41度**しかない。道案内動画はまさに縦持ちで撮るものなので、以前の実装は縦動画の曲がり角の角度を常に1.6倍ほど大きく見積もっていた。現在は対角画角75度を仮定し、そこから縦横比に応じた水平画角を求めている（16:9なら67.2度、9:16なら41.2度）。

この方式の利点:

- 判定の単位が「度」なので、サンプル間隔・解像度・歩く速さに依存しない、意味の分かる閾値になる
- 前進しているだけ（Bが大きくAが小さい）と、実際に向きを変えた（Aが大きい）を原理的に区別できる
- 曲がった角度の推定値が出るので、画面に「約72度 右へ・自信度 高」と根拠を表示できる
- 積分区間は固定1.2秒ではなく、向きが変わり続けている間だけ伸ばす（最大2.6秒）ので、ゆっくり向き直る場合にも対応する
- 上下方向の動きが支配的なブロック（足の着地による上下のブレ）は除外する
- 有効ブロックが少ないサンプルは「判定不能」として、当てずっぽうの結論を出さない

**積分の打ち切り方（3回目の改修で作り直した部分）**
以前は「合計と符号が食い違うサンプルが1つでも来たら、そこで積分を打ち切る」というノイズ対策だった。ゆっくりした曲がり角ではこれで問題ないが、**急な曲がり角ではこの打ち切りが致命的だった**。曲がりの一番きつい瞬間はサンプル間の移動量が最大になり、そこだけ測定が不安定になって符号が2〜3サンプルひっくり返る。すると**一番回転している部分がまるごと捨てられ、落ち着いたあとの小さな残りかす（10度程度）しか残らない**。実機サンプルの1:40の右折が検出できなかった直接の原因はこれだった。

現在は「合計の**最大到達点**を覚えておき、そこから**意味のある割合まで戻したら**打ち切る」方式にした（本当に曲がったなら曲がり戻したりしない、という考え方）。あわせて、その区間が本当に一方向だったかを表す指標（**正味の回転 ÷ 回転量の絶対値の合計**）を計算し、これが低い＝行ったり来たりしているだけのものは、合計が偶然大きくなっても採用しない。

検出する場面は2種類:

1. **1秒以上立ち止まった直後**（初版と同じ考え方。閾値は既定35度）
2. **立ち止まらずに曲がった場合**（新規。誤検出を避けるため閾値を高くし、既定52度。画面のチェックボックスでオフにできる）

2番目については、**動画の最初の2秒と最後の1.5秒を対象外**にしている。撮影者がスマホを構え直している間の動きは曲がり角とまったく同じ見え方をするのに、道順としては何の意味もないため（実機サンプルでは開始1.3〜2.2秒に約40度のパンがあり、これを「右に曲がります」と誤検出していた）。終わり際も、すでに「到着しました」のテロップが出ている場所なので除外している。

「見つけやすさ」プリセットは、この2つの閾値と停止判定の閾値をまとめて動かす。

| プリセット | 立ち止まってから曲がる | 歩きながら曲がる | 停止の判定 |
|---|---|---|---|
| ひかえめ | 45度以上 | 45度以上 | 厳しめ（0.72倍） |
| おすすめ（既定） | 35度以上 | 36度以上 | 標準（1.0度/秒） |
| 多め | 27度以上 | 30度以上 | ゆるめ（1.45倍） |

「歩きながら曲がる」の閾値は、**実測値から決めた**。実機サンプルで全区間の候補を洗い出したところ、それ以外で最大の候補は27度しかなく、大きな隙間があった。

ここで重要なのは、**同じ動画・同じコードでも解析するたびに推定角度が変わる**こと。ブラウザがどのフレームを返すかがそのときの負荷次第なので、サンプル位置が毎回変わる。実機サンプルの右折は、実行ごとに**45〜77度**と大きく振れた（左折は90〜103度）。当初は隙間の中央付近と考えて45度にしていたが、それでは45.1度と出た回に**ぎりぎり検出できるかどうかが運任せ**になってしまう。そこで「本物の下限（45度）」と「偽物の上限（27度）」の間に入る36度まで下げた。閾値を決めるときは、1回の実測値ではなく**複数回まわしてブレ幅を見ること**。

### 左右判定のあいまいさへの対処
符号の向きは合成映像に加えて**実機の動画でも検証済み**（映像が左へ流れる＝カメラが右を向いた＝「右に曲がります」）。`sample/IMG_2886.MOV` の実際の道順（1:40に右折、4:51に左折）と、ツールの出力（1:39に右、4:51に左）が一致した。したがって既定のままで正しい。
それでも「左右が逆に出ている」チェックボックスは残してある。レンズや撮り方によっては将来ずれる可能性があり、非技術者が自分で直せる手段が1つあるほうが安全なため。チェックすると、自動判定・手動修正・既存テロップの文言をまとめて入れ替える。

### テロップの自動生成
- 曲がり角と判定された箇所に「右に曲がります」/「左に曲がります」を自動追加（表示時間 約2.8秒）
- 6秒以上続く「歩いている」区間の頭に「道なりに進みます」を自動追加（ただし曲がり角のテロップと3.5秒以内に重なる場合は出さない）
- 動画の最後の3秒に「到着しました」を自動追加
- 自動生成はあくまで初期値。ユーザーが編集・移動したテロップには印がつき、**設定を変えて判定をやり直しても消えない**（触っていない自動テロップだけが作り直される）

### 早送りの実装

早送りは**2段階**にしてある（4回目の改修）。速さの違う2種類を使い分けるのが要点で、全部を同じ倍速にすると「止まっているところを飛ばす」のと「単調な道を縮める」のが混ざって見づらくなる。

| 種類 | 対象 | 速さ | 既定 |
|---|---|---|---|
| **強い早送り** | 立ち止まっている区間 | 「早送りの速さ」設定（既定4倍） | ON（曲がり角の直前の停止だけOFF） |
| **ゆるい早送り**（クルーズ） | 同じ景色が続く直進が一定時間以上続くところ（既定20秒／10・20・40秒から選択） | 1.5・2・3倍から選択（既定2倍） | ON（チェックボックスでOFFにできる） |

ゆるい早送りは**1章の「止まっている・景色が変わらない区間を早送り」という当初の要件のうち、後半（景色が変わらない）に対応する部分**。3回目の改修で停止の過検出を直した結果、一度も立ち止まらない動画では早送りが一切入らなくなり、5分半の動画が5分半のまま出てくるようになったため、撮影者本人に確認したうえで追加した。

**曲がり角の前後は等倍のまま残す**。これが実装上の一番の注意点で、「立ち止まらずに曲がる」曲がり角はどの停止区間にも紐づいていないため、単純に「長い直進を早送り」とやると、**一番見せたい曲がる瞬間とそのテロップを2倍速で飛ばしてしまう**。そこで曲がり角の前4秒・後5秒を保護区間として、長い直進からその部分を差し引いてから早送り対象を決めている。同じ理由で**動画の最初3秒**（出発地点がどこか分かる部分）と**最後5秒**（「到着しました」のテロップが出る部分）も等倍のまま残す。

保護区間で分割された結果は、**それぞれが独立した区間として一覧に並ぶ**ので、ユーザーは「この直進だけ等倍に戻す」といった操作が普通にできる。

- 区間ごとに一覧からON/OFFを切り替えられる。ユーザーが切り替えた区間は、判定をやり直しても設定が保たれる
- 初版は「歩いている」区間が**すべて**既定で強い早送り（4倍）の対象になっており、道案内動画としては本末転倒だった。今回のクルーズはそれとは別物で、対象を「一定時間以上つづく単調な直進」に限り、速さも控えめにしている

**速さと長さはユーザーが変えられる**（5回目の改修）。最初は決め打ちにしていたが、撮影者から「固定はしたくない、自分で調整できるようにしてほしい」という要望があったため設定画面に出した。UIは既存の「早送りの速さ」と同じ**プリセットボタン方式**（自由入力の数値欄は作らない）で揃えてある。

- **速さ**：1.5倍 / 2倍 / 3倍
- **長さ**：10秒 / 20秒 / 40秒（「どのくらい続いたら早送りするか」）
- どちらもチェックボックスの下にぶら下げて、OFFのときは薄く表示して操作できないようにしている
- 選択中の値は「20秒より長くまっすぐ歩いている所を2倍速にします。」という文章に組み立てて常時表示する。数字だけを見せない（1章の方針）
- どちらも**解析はやり直さず、その場で区間を作り直す**。動き自体の測定結果は変わらないため。とくに**速さの変更は区間の境界を1つも動かさない**ので、区間の作り直しすらせず再描画だけで済ませている（ユーザーが区間ごとに設定した手動の早送りON/OFFが、速さを変えただけでリセットされないようにするため）
- プレビューは `video.playbackRate` で確認。「早送りを反映して再生する」のチェックを外せば全編等倍で中身を確認できる
- 早送り区間の音は既定で消す

### 書き出し（ここが設計上の一番のポイント）
当初はffmpeg.wasmでの書き出しも検討したが、以下の理由から**Canvas + MediaRecorder方式**を採用した。この判断は変更していない。

- ffmpeg.wasmで日本語テロップを焼き込むには、CJK対応フォント（Noto Sans JPなど）をffmpeg側に読み込ませる必要があるが、安定して動作するCDNのURLをこの場で実地検証する手段がなかった（ネットワークアクセスができない開発環境だったため）。誤ったURLを埋め込むと、ユーザーの手元で初めて壊れているのが分かるリスクがあった
- 一方でCanvas + MediaRecorderなら、ブラウザ標準の日本語フォントレンダリングがそのまま使えるため、外部フォント取得が一切不要
- 実装：オフスクリーンcanvasに動画フレームを`drawImage`し、その上にテロップをCanvas 2D APIの`fillText`で直接描画。`canvas.captureStream()`で映像トラックを取得し、`MediaRecorder`に渡して録画
- **音声はWeb Audio API経由に変更した**。`MediaElementAudioSourceNode` →（監視用ゲイン／録音用ゲイン）→ `MediaStreamAudioDestinationNode` という経路にして、早送り区間では録音用ゲインを0にする。`video.muted` の切り替えに頼ると、ブラウザによって `captureStream()` の音声トラックに反映されるかどうかが違うため。Web Audioが使えない環境では従来の `captureStream()` 方式にフォールバックする
- テロップが長い場合は画面幅に合わせて**自動で折り返す**（初版は画面外にはみ出していた）
- 書き出しは実際に動画を（早送りも含めて）最後まで再生しながら録画する方式なので、書き出しにかかる時間は編集後の動画の長さとほぼ同じ。書き出し中に別のタブへ移ると映像が途切れるため、その旨を画面に表示し、実際に移動したら警告を出す
- 対応ブラウザはChrome / Edge推奨（Safariは`captureStream`まわりの対応が不安定なため）

### UI（2回目の改修で作り直した部分）

配布先が「パソコンに詳しくない家族・知人」であることを踏まえ、開発者向けに見えていた部分を作り直した。

- **配色**：暗いターミナル風の配色・等幅フォント・英字の大文字見出しをやめ、明るい紙のような配色にした。等幅フォントはタイムコードだけに使う
- **説明の出し方**：初版はヘッダーにアルゴリズムの説明が長文で置かれていた。1行の説明＋折りたたみの「このツールについて／うまく作るコツ」に分け、そこに「曲がり角では一度立ち止まってゆっくり向き直るときれいに検出される」といった**撮り方のコツ**も書いた
- **設定**：意味の分からない「検出の感度 30」というスライダーを廃止し、**「ひかえめ／おすすめ／多め」の3択**にした。早送り倍率も「2倍／4倍／6倍／8倍」の選択式。ゆるい早送りの速さ・長さも同じプリセットボタン方式で統一している（自由入力の数値欄はどこにも作らない）。各項目に「?」のツールチップで、何が起きるかを日本語で説明
- **ステップ表示**：現在地が分かるようにし、その下に「いま何をすればいいか」を1行で出す
- **解析中**：進捗％と残り時間の目安、中止ボタン、「タブを開いたままにしてください」の注意書き
- **結果の要約**：「早送りにする区間：6か所（合計 42秒 → 約 11秒）」「曲がり角：3か所」のように、専門用語なしで結果を伝える。立ち止まりが見つからなかった／曲がり角が0件だった場合は、次に何をすればいいかを案内する
- **区間一覧（新規）**：初版は区間の操作がタイムラインのクリックだけで、しかもクリックすると再生位置移動ではなくON/OFFが切り替わるという分かりにくい挙動だった。現在は
  - タイムラインのクリック＝**その時間へ移動**（本来期待される動作）
  - 右側の一覧に区間が並び、それぞれに「ここを見る」ボタン（該当箇所だけを等倍で再生）、「早送りする」チェック、曲がり角の向き（右へ／左へ／曲がっていない）の選択がある
  - **自動判定が外した曲がり角を手で追加できるし、誤検出を消せる**。手で直した内容には「（手動）」と表示され、設定を変えても保たれる
  - 自動判定には「約72度・自信度 高／中／低」を併記し、**どこを重点的に確認すればよいか**が分かるようにした
  - 再生中は現在位置の区間・テロップがハイライトされる
- **タイムライン**：高さを増やし、時間目盛りと、動き量のグラフ・判定に使った閾値の線を重ねた。早送り区間は斜線で示す。マウスを乗せると区間の内容がツールチップで出る
- **テロップ**：
  - 開始・終了時間の入力を検証するようにした（初版は終了＜開始や動画長を超える値がそのまま入った）
  - 「上／中央／下」の位置プリセットと「今の時間から表示」ボタンを追加（縦動画ではドラッグがしづらいため）
  - プレビューの**アスペクト比を実際の動画に合わせた**。初版は16:9固定だったため、縦動画では黒帯を含む枠を基準に位置を決めてしまい、書き出し結果とずれていた。文字サイズも書き出しと同じ比率にしたので、見たままが出力される
- **操作性**：スペースキーで再生/一時停止、左右キーで5秒送り戻し。「もう一度解析する」「別の動画を選ぶ」ボタン。ドラッグ中にテロップ表示が再構築されない対策

### UI（6回目の改修：ステップ3「確認して直す」の作り直し）

解析の精度が実用になったあと、撮影者から**「解析した後、確認して直すのがやりづらい」**という指摘があり、次の2点を直した。どちらも「見ている場所」と「操作する場所」がずれているのが原因だった。

**(1) テロップを映像の上で直接編集できるようにした**

それまでは、テロップの位置だけは映像上でドラッグできるのに、**文字の修正と削除は右側の一覧までスクロールして該当行を探す**必要があった。ユーザーは映像の上のテロップを見て「これを直したい」と思うのに、操作は別の場所、という分断になっていた。

- テロップを**クリックするとその場に小さな編集パネルが開く**（文字の入力欄／位置プリセット 上・中央・下／削除／閉じる）
- 押した指が数px以上動いたときだけドラッグ扱いにして、**ただのクリックは編集、動かしたら移動**と自然に切り替わるようにした。これが無いと、クリックしたつもりでテロップが1pxずれるだけで編集が開かない
- 編集パネルはプレビュー枠の外にはみ出さないよう、テロップが画面下寄りなら上側に、上寄りなら下側に自動で回り込む（プレビュー枠は`overflow:hidden`なのではみ出すと切れてしまうため）
- **右側の一覧との同期は双方向**。どちらで文字を直しても両方に反映される。一覧は残してあり、表示時間を秒単位できっちり決めたいときはそちらを使う（役割を「一覧＝時間の調整」と書き分けた）
- 「＋ 今の位置にテロップを追加」を押すと、追加と同時に編集パネルが開いて入力欄にカーソルが入る
- テロップが編集できることが分かるように、**右上に小さな鉛筆マークを常時表示**している。ホバーで出す方式にしなかったのは、タッチ操作の人には見えないため。マークは絵文字ではなくSVGで描いている（絵文字だと環境によっては豆腐□になる。実際ヘッドレスChromeでの確認時にそうなった）

**(2) 「書き出す」ボタンを画面下に固定した**

それまで書き出しパネルは右カラムの一番下にあり、区間やテロップが増えるほど下に押し下げられて、**スクロールしないと見つからない**状態だった。ステップ表示に「4. 書き出す」と書いてあるのに、その4番目がどこにあるか分からないという状態になっていた。

- 画面下に固定バーを置き、そこに**書き出し後の長さ・進捗・状態・ダウンロードリンク・書き出しボタン**をまとめた。スクロール位置に関係なく常に見える
- ボタンを2箇所に置くと迷わせるので、右カラムの書き出しパネルは**廃止**して固定バー1つに集約した
- 固定バーの分だけ`body`に下余白を入れて、最後のパネルがバーの下に隠れないようにしている
- 長い注意書き（「書き出しには実時間かかる／タブを開いたままに」）は、他の設定項目と同じ「?」ツールチップに入れて、バー自体は1行に収めた

### UI（7回目の改修：スマホ対応）

「URLを送るだけで渡せる」のが売りなので、**受け取った人がスマホで開く**ことは十分あり得る。それまでは2カラムを1カラムに畳む指定と、固定バーを少し縮める指定しか無かったため、実機幅（375 / 430 / 768 / 1280 / 1600 px、および横持ち 812×375）で実際に描画して測り直した。直したのは次の点。

- **ステップ表示（1〜4）が画面からはみ出していた**。375pxでは幅498px必要で、横スクロールの帯になっており、「3. 確認して直す」「4. 書き出す」が見えないうえ、そこにあること自体が分からなかった。560px以下では**2×2のグリッド**にして4つとも見えるようにした
- **タップ目標が小さかった**（最小31px）。700px以下と`pointer:coarse`（タッチ端末）で、小さいボタン・プリセットボタン・`<select>`・数値入力の余白を広げた（最小39px）。「上」「下」のような1文字ボタンには最小幅も指定した
- **テロップ一覧のボタンが単語の途中で改行していた**（「中央」が「中」「央」に割れる等）。折り返しを許可しつつ、ボタン内では改行しないようにした
- **テロップ編集パネルを、映像の枠ではなくプレビューのパネルに対して配置するようにした**。横持ちスマホで縦動画を開くと映像の表示幅が130px程度しかなく、枠内に収めようとするとパネルがつぶれて操作できなかった（ボタン列が122pxはみ出していた）。映像の枠は`overflow:hidden`なので、はみ出す分は切れてしまう。パネル基準にしたことで、映像が小さくても編集パネルは常に320px確保できる
- **横持ち（画面が低い）とき、ヘッダーだけで画面の7割が埋まっていた**。`max-height:520px`で説明文とプライバシー表示を隠し、余白とステップ表示を詰め、固定バーも薄くした。あわせて映像の高さ上限を62vh→80vhに緩めた
- **「右側の一覧から…」といった文言をやめた**。1カラムになると一覧は右ではなく下に来るため、位置ではなく「『自動判定の結果』の一覧」のようにパネル名で案内するようにした
- ホバーでしか出ない操作は無いことを確認済み（ツールチップは`:focus`でも開き、タイムラインのホバー説明は同じ情報が一覧にも出ている）。ドラッグ・タイムライン操作はポインタイベントなのでタッチでもそのまま動く

### UI（8回目の改修：書き出し完了後に固定バーが縦に壊れる不具合）

実機のiPhone（Safari）で、動画を書き出したあとに画面全体が壊れる不具合が報告された。「書き出しが完了しました。WebM形式です…」という案内文が1文字ずつ改行され、画面の何倍もの高さに伸びていた。

原因は固定バー（`.action-bar-inner`）のflexレイアウト。書き出し前は「文言＋書き出すボタン」の2つしか横に並ばず問題なかったが、**書き出し完了後はそこに「ダウンロードボタン」が増える**。ダウンロードボタンは`white-space:nowrap`で縮まないため、スマホ幅では文言側（`.ab-info`）にほぼ幅が残らない。当時`.ab-info`には`min-width:0`（縮んでよい、という指定）しか入っておらず、幅ゼロ近くまで押しつぶされた結果、スペースを含まない日本語がそこで1文字ずつ改行され、要素の高さが際限なく伸びた。7回目の改修時のスマホ確認は「書き出し前」の状態しか見ておらず、この組み合わせを見逃していた。

対応：
- `.action-bar-inner`に`flex-wrap:wrap`を追加し、幅が足りなければ折り返せるようにした
- `.ab-info`は`min-width:0`をやめ、`flex:1 1 240px;min-width:200px`という**実際の下限**を持たせた。下限を割るときは押しつぶすのではなく次の行に折り返す
- スマホ幅（700px以下）では`.ab-info`を独立した1行（`flex-basis:100%`）にし、ダウンロードボタン・書き出すボタンは次の行で幅を分け合う
- 万一の保険として`.action-bar`に`max-height:55vh;overflow-y:auto`を追加。想定外の折り返しが起きても画面全体は壊れない
- 書き出しが終わったら「書き出す」ボタン自体を隠すようにした（ダウンロードするだけなら不要なうえ、狭い画面でボタンが1つ減る）
- 固定バーの高さは可変になった（1行/2行、ダウンロードリンクの有無で変わる）ため、`body`の下余白は決め打ちをやめ、実際の高さを測って合わせるようにした（`syncActionBarSpace()`）
- ついでに、書き出し後にテロップや区間設定を変更したらダウンロードリンクを消して「もう一度書き出してください」と出すようにした（内容とズレたファイルをそのまま渡してしまう事故を防ぐ）

検証は`tools/export-bar-check.mjs`を新設。書き出し完了直後のDOM状態を再現（または`--real`で実際に書き出しを実行）し、375/430/768/1280pxそれぞれで文言が1文字ずつ改行されていないか（1行あたりの文字数）、バーが画面の何%を占めるか、横スクロールが出ていないかを数値で確認する。**「書き出し前」の画面しか確認しないと今回のような不具合を見逃す**ため、UIを触ったときは書き出し後の状態も必ず確認すること。

### UI・書き出し（9回目の改修：出力をmp4にし、スマホの「写真」に保存できるようにした）

実機のiPhoneで書き出したところ、**ファイルが `.webm` で出てきて、しかも「ファイル」アプリに落ちるだけだった**という報告があった。撮影者が期待していたのは「写真」アプリに入って、そのまま見たり送ったりできることだった。原因は独立した2つで、両方を直した。

**(1) 出力形式をmp4優先にした**

それまでの `pickMimeType()` はwebmを先に聞いていた（`video/webm;codecs=vp9,opus` → … → 最後に `video/mp4`）。書ける環境なら必ずwebmが選ばれる並びで、しかも**実機のiPhoneから出てきたファイルが `.webm`（391.1MB）だった**ことから、最近のiOS SafariはMediaRecorderでwebmも書けると分かる（`isTypeSupported` がwebm候補のどれかに `true` を返している。実機のコンソールで確認したわけではなく、出てきたファイルからの逆算）。これはスマホでは最悪の結果で、**iOSはwebmを「動画」として扱わない**ので、共有シートに「ビデオを保存」が出ず、写真アプリに入れられない。

そこで候補の並びを**mp4（H.264/AAC）優先・webmはフォールバック**に入れ替えた。あわせて次の点を直した。

- 候補文字列は**具体的なものから順に**並べる（`video/mp4;codecs=avc1.42E01E,mp4a.40.2` → `…;codecs=avc1,mp4a.40.2` → `…;codecs=avc1` → `video/mp4`）。ブラウザによって完全修飾の文字列しか受け付けないもの／逆にコンテナ名だけしか受け付けないものがあるため
- **`codecs=h264` は使わない**。実測でChromeは `h264` / `h264,aac` を `false` と答え、`avc1` 系だけを `true` と答えた（この手のことは想像で書かず、`tools/export-format-check.mjs` で実際に聞くこと）
- `isTypeSupported()` が `true` を返しても**コンストラクタが受け付けるとは限らない**ので、対応していると答えた候補を上から順に `new MediaRecorder` に渡し、全部失敗したときだけブラウザ既定に落ちるようにした。既定に落ちた場合は `recorder.mimeType` を信用して拡張子を決める
- mp4が書けない環境（Firefox、古いChrome）は**これまでどおりwebmになる**ので後退はない

副作用として、**Chrome/Edgeでの出力もmp4に変わる**。これは望ましい変化で、4章にあった「出力形式はwebmになる可能性が高く、mp4を期待する人には一手間かかる」という制限がそのまま解消される。

**(2) 「保存・共有」ボタン（Web Share API）を追加した**

mp4にしただけでは足りない。**`<a download>` はiOS Safariでは「ファイル」アプリにしか入らない**。任意のWebコンテンツをカメラロールに直接書き込むAPIは、OS側のプライバシー方針で存在しない。Webページから写真アプリに届く唯一の経路は**ファイル付きのWeb Share API**（`navigator.share({files:[File]})`）で、これがネイティブの共有シートを開き、そこに出る「ビデオを保存」で写真アプリに入る。

- 判定は必ず **`navigator.canShare({files:[file]})`** で行う。`navigator.share` は在るのにファイルは拒否する、というブラウザがあるため
- 共有する `File` の MIME は**コーデック情報を落とした素のコンテナ型**（`video/mp4`）にしている。OSは受け取った型で「これは動画か」を判断するため。Blobを作る時点でこの型にしてある
- `navigator.share()` は**クリックハンドラの中から同期的に呼ぶ**。`await` をはさむとiOSはユーザー操作が消費されたとみなして拒否する。そのため `File` はボタンを作る時点で用意しておく
- ユーザーが共有シートを閉じただけのとき（`AbortError` / `NotAllowedError`）は**エラー扱いにしない**。それ以外の失敗のときだけ「ダウンロードから保存してください」と案内する
- **ダウンロードリンクは残す**。共有が使えない環境のためであり、共有シートがうまく動かなかったときの逃げ道でもある

**どちらを主役にするかは `(pointer:coarse)` で決めている**。これは実測から必要になった判断で、**`file://` で開いたデスクトップChrome（Windows）も `navigator.canShare({files})` に `true` を返す**（`file://` はsecure contextとして扱われる）。つまり単純に「共有が使えるなら共有を主役」にすると、パソコンで使っている既存ユーザーの主役ボタンがダウンロードから共有に変わってしまう。パソコンでやりたいのはダウンロードなので、

- タッチ端末（`pointer:coarse`）… 「保存・共有」が主役（塗り）、「ダウンロード」が控え（白抜き）
- それ以外 … これまでどおり「ダウンロード」が主役、「保存・共有」が控え

とした。案内文もそれに合わせて出し分ける（タッチ端末では「『保存・共有』→『ビデオを保存』で写真に入ります。」）。

**固定バーのレイアウト**（8回目の改修で壊れた場所そのもの）については、ボタンが1つ増えるので次の手当てをした。

- `#download-area` を `display:flex;flex-wrap:wrap` にして、ボタン同士が折り返せるようにした
- 主役ボタンのラベルから拡張子を落として `⬇ ダウンロード（391.0MB）` まで短くした。形式は案内文に書いてあるので重複しており、**短くしたことで375px幅でも2つのボタンが1行に収まる**（長いままだと2行になり、バーの高さが161px→211pxに増えていた）
- 書き出しが終わったら「?」ツールチップも隠すようにした。中身が「書き出しには実時間かかる／タブを開いたままに」という**もう終わったことへの注意**であり、狭い画面ではこれだけが3行目に押し出されていたため。`invalidateExport()` で書き出しボタンを戻すときに一緒に戻す

**(3) 「パソコンのほうが速い」の一文を追加**

実機のiPhoneで使った撮影者から「動作が重い」という感想があった。壊れているわけではなく、解析（動画の長さの約1/3）と書き出し（できあがりの長さとほぼ同じ）を1つのタブの中でやるため、CPUの弱いスマホでは単純に待ち時間が長くなるというだけ。警告バナーのような強い出し方はせず、ヘッダーの折りたたみ「このツールについて／うまく作るコツ」に、既存の注意書きと同じ調子で1項目足した。

**(4) 検証しやすさのための小さなリファクタ**

`exportVideo()` の末尾（ダウンロードリンク・共有ボタン・案内文を組み立てる部分）を `showExportResult(blob, mimeType)` として切り出し、`window.__showExportResult` で公開した（`window.__debug` と同じ理由・同じ扱い）。これにより `tools/export-bar-check.mjs` の高速モードが、**手書きで再現したDOMではなく本物のコードが作るDOM**を測れるようになった。手書きの再現は本体と食い違っても誰も気づかないという弱点があり、まさに8回目の改修で見逃したのがその種の不具合だった。

## 4. 現状の既知の制限・トレードオフ

- 曲がり角の判定はあくまでピクセルの動きだけを見た推定。GPSやジャイロなどのセンサー情報は動画ファイルに含まれていないため使っていない
- 画角は「対角75度」を仮定し、そこから動画の縦横比で水平画角を計算している。実際の画角はレンズや機種（超広角で撮った場合など）で変わるため、角度の推定値には誤差が出る。閾値には余裕を持たせてあるので、通常の曲がり角（90度前後）の検出には影響しにくい（実機サンプルでは90度前後の右折・左折がそれぞれ59〜75度、103度と推定された）
- **同じ動画を解析しても結果は毎回変わる**。フレームがどのタイミングで届くかがブラウザのデコード状況次第のため、サンプル位置が毎回変わる。実機サンプルでの右折の推定角度は実行ごとに**45〜77度**と振れた（閾値36度に対しては余裕があり、検出の有無自体は安定している）。区間の数や境界も1〜2箇所ぶん前後する
- **カメラを回さずに体だけ曲がるような撮り方**（進行方向は変わるがカメラは同じ向きのまま）は原理的に検出できない
- **歩きながらスマホを振り向かせただけ**（実際には曲がっていない）を「曲がった」と誤検出する可能性がある。これは映像だけでは区別できない。誤検出が気になる場合は「立ち止まらずに曲がった所も探す」をオフにする
- 画面の大部分がフェンス・タイル・横断歩道のような繰り返し模様で埋まっている場合、判別不能として棄却されるブロックが増え、その区間の判定精度が落ちる（でたらめな判定はしないが、検出できないことがある）
- 極端に暗い映像、濃霧、真っ白な壁の前などでは有効ブロックが取れず、輝度差ベースの簡易判定にフォールバックする。この経路は自動露出の変化に弱い
- 走る・自転車など、歩行よりかなり速い移動では、広げたほうの探索範囲（±91px）も超えて動きを過小評価することがある。曲がり角の検出自体は成立するが、推定角度は小さめに出る
- 解析には**動画の長さの約1/3の時間**がかかる（5分半の動画で約110秒）。以前は約1/6だったが、フレームの間隔を詰めないと急な曲がり角が測れないと分かったため、再生速度を自動的に落とすようにした結果として伸びた。`requestVideoFrameCallback` が使えない環境ではシーク方式になり、さらに時間がかかることがある
- **停止判定の絶対閾値（1.0度/秒）は実機1本ぶんの実測で決めたもの**。手ぶれの大きい撮り方や、極端に暗い・霧などで測定値そのものが持ち上がる映像では、本物の停止を取り逃がす可能性がある。その場合は「見つけやすさ」を<b>多め</b>（1.45度/秒）にすれば拾いやすくなる
- クルーズ（ゆるい早送り）の速さ・長さは選択肢が3つずつの決め打ちで、動画の内容に応じて自動調整はしない。用意した範囲（1.5〜3倍／10〜40秒）の外に出したい場合は、`CFG.CRUISE_SPEED` / `CFG.CRUISE_MIN_LEN` の初期値かボタンの選択肢自体を増やす必要がある
- 「同じ景色が続く」の判定は実際には**「立ち止まっていない直進が一定時間以上続いた」**という意味でしかない。景色そのものが似ているかどうかは見ていないので、変化に富んだ道でも時間さえ続けば早送り対象になる
- 音声トラックの取得はブラウザ依存の可能性が残る（Web Audio経由に変えて改善したが、実機未検証）
- 出力形式は**mp4（H.264/AAC）を優先**するようになった（9回目の改修）。**Chromeでmp4が書き出せることは実測で確認済み**（`ftyp`署名あり・読み戻して長さと解像度が取れる）。**iPhone実機でも確認済み**：実際にiPhoneで書き出したところmp4が選ばれ、「保存・共有」→「ビデオを保存」で写真アプリに保存できることをユーザー本人が確認した。**mp4のMediaRecorderを持たないブラウザ（Firefoxなど）は従来どおりwebmになる**ので、その場合はmp4を期待する人に一手間（変換）が残る
- **iPhoneで「写真」アプリに保存できることは実機で確認済み**（9回目の改修直後、ユーザーが実際にiPhoneで書き出し、「保存・共有」→「ビデオを保存」から写真アプリに保存できることを確認した）。ただし確認できたのは1台の実機・1回の書き出しのみで、機種・iOSバージョンによる違いまでは検証していない
- ファイル付きWeb Share APIに対応していないブラウザ（多くのデスクトップ、一部のAndroid）では「保存・共有」ボタン自体が出ず、ダウンロードのみになる（従来どおり）
- 書き出しにはできあがりの動画の長さとほぼ同じ時間がかかり、解析には元の動画の約1/3の時間がかかる。どちらもタブの中で動くので、**CPUの弱いスマホでは体感がかなり遅い**（壊れてはいない）。折りたたみの説明でパソコンを勧めている
- 書き出し中に別のタブへ移るとフレームが落ちる（`requestAnimationFrame` がブラウザ側で止まるため）。警告は出すが防げない

## 5. 次にやると良さそうなこと（相談しながら決めたい）

- **クルーズの効き具合を実際に見て確かめる**。実機サンプルでは既定（20秒・2倍）で5分26秒→約2分59秒になる（1.5倍なら約3分47秒、3倍なら約2分10秒、OFFなら約5分24秒）。速さ・長さはユーザーが選べるようにしたので、既定値が妥当か、選択肢の幅（1.5〜3倍／10〜40秒）で足りるか、曲がり角の前後4〜5秒の等倍区間で足りているかを、書き出した動画を見て判断したい
- **別の実機動画でも確認する**。現状の実機検証は縦持ちの1本だけ。とくに（1）横持ちで撮ったもの、（2）曲がり角がもっと多いもの、（3）実際に信号待ちなどで止まっているもの（停止検出が効くことの確認）、（4）走った・自転車など速い移動、で確認したい
- 誤検出／検出漏れのパターンを集めて、プリセットの3段階の値を実データに合わせて調整する
- 早送り倍率やテロップの表示秒数を、区間ごとに個別調整できるようにする（現状はグローバル設定のみ）
- 重い処理をWeb Workerに逃がして、解析中のUIをさらに滑らかにする
- ~~iPhone実機で「保存・共有」を押して写真アプリに入るところまで確認する~~ → **確認済み**（9回目の改修直後にユーザーが実機で確認）
- 書き出したmp4を実際にAndroid/Windows等の他の端末・プレイヤーでも再生してみて、H.264/AACの組み合わせがどこでも問題なく開けるか確認する（iPhoneでは確認済み）
- コードを単一HTMLファイルのままにするか、複数ファイルに分割してメンテしやすくするか（現状は「配布のしやすさ」を優先して単一ファイルを維持）

## 6. 検証のしかた（ビルド・テスト環境が無いため）

このプロジェクトにはビルドもテストランナーも無い。改修時は最低限、次を行うこと。

- **構文チェック**：HTMLに `window.onerror` を仕込んだコピーを作り、`chrome --headless=new --dump-dom` で読み込んでエラーが出ないことを確認する
- **アルゴリズムの単体テスト**：HTMLからJSの該当部分を切り出し、合成画像（多重オクターブのバリューノイズ）を既知の量だけずらしたものを与えて、
  - 移動量が正しく復元されるか（±36px程度まで）
  - 明るさを変えただけのフレームで移動量が0になるか
  - 模様の無い領域・繰り返し模様が棄却されるか
  - 一部を別の絵で覆っても（通行人の再現）中央値が壊れないか
  を確認する
- **判定ロジックのシナリオテスト**：サンプル列を合成して「立ち止まって右折」「信号待ちのあと直進」「立ち止まらず左折」「一度も止まらない」などを与え、期待どおりの区間・テロップになるか確認する
- 上記の検証は実施済みで、いずれも期待どおりの結果になっている（合成映像での曲がり角の推定は、真値192度に対して190度）
- **合成映像だけでは足りない**、というのがこのプロジェクトで得た一番大きな教訓。合成テストは全部通っているのに、実機の動画では曲がり角の半分を取り逃していた。原因は合成テストが再現していなかった条件（縦動画で水平画角が狭いこと、ブラウザのデコードが追いつかずフレーム間隔が開くこと、その結果1サンプルあたりの移動量が探索範囲を超えること）に集中していた。**アルゴリズムを触ったら必ず下記の実機確認まで行うこと**
- **実機動画での通し動作確認**：`tools/verify-real-video.mjs` を使う。実際のChromeを起動して`michi-annai-editor.html`を開き、`sample/IMG_2886.MOV`を読み込ませ、解析が終わるまで待って結果をJSONで出力する。

  ```
  cd tools
  node verify-real-video.mjs                 # 区間・曲がり角・早送りの内訳
  node verify-real-video.mjs --verbose       # 動き量の分布・停止閾値・区間ごとの中央値も出す
  node verify-real-video.mjs --check-toggle  # 早送りの設定を切り替えて結果が変わることを確認
  ```

- **編集UIの動作確認**：`tools/ui-check.mjs`。数値が正しいかではなく「クリックしたときに期待どおり動くか」を見る。テロップをクリックして編集パネルが開くか、ドラッグでは開かずに移動するか、入力が映像と一覧の両方に反映されるか、書き出しボタンがスクロール位置に関わらず見えているか、を実際に操作して確認し、スクリーンショット（`ui-editor.png` / `ui-editor-typed.png` / `ui-badge.png`）も保存する。**UIを触ったら見た目を必ず目視すること**（鉛筆マークが環境によって豆腐□になっていた件は、スクリーンショットを見て初めて気づいた）

  ```
  cd tools
  node ui-check.mjs [出力先フォルダ]
  ```

  **`verify-real-video.mjs` と同時に走らせないこと**（後述のリソース競合）

- **画面サイズごとの確認**：`tools/responsive-check.mjs`。解析を1回だけ実行したあと、ビューポートを切り替えながら「はみ出している要素」「画面右端をはみ出した要素」「一番小さいタップ目標」「編集パネルが収まっているか」を測り、各サイズのスクリーンショットを保存する。

  ```
  cd tools
  node responsive-check.mjs [出力先フォルダ] --shots
  ```

  **注意**：`page.setViewport()` に `isMobile` / `hasTouch` を渡さないこと。途中でモバイルエミュレーションを切り替えるとページが再読み込みされ、解析済みの動画が失われて全パネルが0×0になる（原因が分からず一度ハマった）。幅と高さだけなら状態を保ったまま再レイアウトされる。そのためタッチ端末専用の指定（`@media (pointer:coarse)`）はこの方法では検証できないので、同じ内容を幅ベース（`max-width:700px`）にも書いて、そちらで確認している

- **書き出し完了後の固定バーの確認**：`tools/export-bar-check.mjs`。8回目の改修（書き出し後に文言が1文字ずつ改行される不具合）を受けて新設。既定では**ページ自身の `window.__showExportResult()` をダミーのBlobで呼んで**完了状態を作る（9回目の改修で変更。以前はDOMを手書きで再現していたが、本体とズレても気づけないため）。`--real`を付けると実際に書き出しを最後まで実行してから確認する（動画の長さぶん時間がかかる）。375/430/768/1280pxそれぞれで、バーの高さ・文言の1行あたり文字数・横スクロールの有無・各ボタンの表示状態とビューポート内に収まっているかを数値で出す。**「書き出し前」の画面だけを見て良しとしないこと**（7回目の改修時はまさにこれで見逃した）。

  ```
  cd tools
  node export-bar-check.mjs [出力先フォルダ] --shots            # 高速（本体のコードで完了状態を作る）
  node export-bar-check.mjs [出力先フォルダ] --coarse --shots   # タッチ端末（共有ボタンが主役）
  node export-bar-check.mjs [出力先フォルダ] --no-share         # 共有APIが無い環境（ダウンロードのみ）
  node export-bar-check.mjs [出力先フォルダ] --real --quick --shots  # 実際に書き出す（推奨）
  node export-bar-check.mjs [出力先フォルダ] --real --shots          # 全編を書き出す（このPCでは完走しない）
  ```

  **`--real` を素で走らせても完走しない**（9回目の改修で判明）。ヘッドレスChromeは1080×1920のサンプル動画を早送りしながらデコードし続けられず、書き出し開始から数秒で`<video>`が進まなくなる（`paused`/`waiting`のまま。`pause()`はどこからも呼ばれていない）。**mp4化する前のコードでもまったく同じ**なので、ツールではなく検証環境側の制限。以前の回の`--real`が15分でタイムアウトしていたのもこれが原因だった。`--quick`は15秒録画したところで動画を末尾付近へ飛ばし、止まっていたら`play()`で押し直して`ended`まで到達させる。**同じMediaRecorder・同じcanvas・同じBlob・同じ完了画面**を通るので、書き出し経路の確認としては十分に実物。

  `--coarse` は `window.matchMedia('(pointer:coarse)')` を差し替えて判定させている。`page.setViewport()` に `hasTouch` を渡すとページが再読み込みされて解析結果が消える（上の responsive-check の注意と同じ罠）ため、エミュレーションではなくコードが実際に見ている一点だけを偽装する。

- **書き出し形式と共有APIの確認**：`tools/export-format-check.mjs`（9回目の改修で新設）。`michi-annai-editor.html` から `MIME_CANDIDATES` を**読み出して**（配布ファイルとズレないように）、各候補に `MediaRecorder.isTypeSupported()` が何と答えるかを一覧で出し、選ばれた形式で実際に3秒だけ録画して、(1) コンテナの署名が正しいか（mp4なら `ftyp`、webmならEBML）、(2) `<video>` に読み戻して長さと解像度が取れるか、(3) `navigator.canShare({files})` が何を返すか、を確認する。

  ```
  cd tools
  node export-format-check.mjs
  ```

  **確認できないこと**：実機のiOS Safari。`isTypeSupported` が向こうで何と答えるかも、共有シートに「ビデオを保存」が出るかも、ここからは分からない。**iPhoneが絡む変更は必ずその旨を明記すること**（なお、mp4優先＋Web Share APIの組み合わせ自体は、9回目の改修直後にユーザーが実機で確認済み。以降iPhone関連のコードを変更した場合は、この確認結果がまだ有効かどうか改めて実機で見てもらうこと）。

  `tools/` は**開発用のみ**で、配布物には含まれない（配布物は単一HTMLのまま）。`puppeteer-core`を使っているのでChromiumの追加ダウンロードは発生せず、既存のChromeをそのまま使う。
  - **同時に2つ実行しないこと**。動画をデコードするChromeを2つ並行で走らせると、リソースを奪い合って片方が "frame got detached" で落ちる
  - `--disable-gpu` を付けないこと。付けるとヘッドレスで動画のデコードが止まり（`currentTime`が進まない）、解析が「samples.length<6」で失敗する
  - Node.jsが無い環境向けに、以前はPowerShellの`System.Net.WebSockets.ClientWebSocket`でCDPに直接つなぐスクリプトを使っていた。同じことはできるが壊れやすいので、Nodeが使えるならこちらを使う。当時ハマった点（次に手書きする人向け）：**ページのスクリプトが実行され終わるまで待ってからファイルをセットする**（`#file-input`がDOMに在るかどうかだけで判断すると、まだリスナーが付いていない段階でセットしてしまい`change`が誰にも拾われない）／**`DOM.setFileInputFiles`は自分で`change`を発火する**ので二重に投げない／`DOM.enable`を付けっぱなしにするとDOM更新イベントで詰まる／`DOM.getDocument`の`nodeId`はページが書き換わると無効になる
  - 解析結果を外から読めるように、`startAnalysis()` の最後で `window.__debug`（サンプル列・区間・曲がり角・閾値・`integrateYaw` など）を公開している。同じ理由で、書き出し完了後の画面を作る関数を `window.__showExportResult` として公開している（9回目の改修）。どちらもユーザーの動作には影響しないので、そのまま残してある。実機で何が起きているかを数字で見られるようにしておかないと、この種の不具合はまず直せない
  - **実機での確認結果（最新）**：5分26秒・1080×1920の縦動画が例外・コンソールエラー無しで最後まで解析完了（所要 約80〜120秒）。
    - 曲がり角：**実際の道順（1:40に右折、4:51に左折）に対して、1:39に右・4:51に左をそれぞれ検出し、それ以外の誤検出は0件**
    - 停止：撮影者本人の「ほとんど立ち止まっていない」という証言に対し、**停止と判定した区間は1〜2箇所・合計1〜3秒**（いずれも動画中で最も動きの少ない瞬間）。3回目の改修前は33箇所・合計約115秒だった
    - 早送り：クルーズ3箇所（3〜90秒／105〜287秒／298〜321秒）が2倍速、停止1箇所が4倍速。曲がり角の前後・冒頭・末尾は等倍のまま。**書き出し後の長さは約3分（元5分26秒）**
  - **9回目の改修時の実測（mp4化のあと）**：
    - 解析：108秒で完了。曲がり角は 99.3秒に右（51.5度）・291.1秒に左（99.2度）の2件のみで、上の基準と一致（誤検出0件）
    - 書き出し：`export-bar-check.mjs --real --quick` が完走し、**`video/mp4` / `ftyp` 署名あり / 42.4MB / 読み戻して 1080×1920・108秒** を確認。完了画面のボタンは「ダウンロード（40.4MB）」＋「保存・共有（控え）」で、375/430/768/1280pxいずれも横スクロールなし・文言は2行以内・バーは画面の20%以下
    - 書き出し前のレイアウトは、`responsive-check.mjs` の全項目（はみ出し・最小タップ目標・固定バーの高さ・`body`下余白）が**改修前の値と完全に一致**
    - `export-format-check.mjs`：1080×1920のcanvasを5秒録画して 5.01秒・56fps・flush 0秒。**mp4エンコーダ側は等倍以上で余裕がある**（上記の停止はデコード側の問題であることの裏付け）
- **数字を見ずに直そうとしないこと**。停止の過検出のときも、原因は「探索範囲を広げたせいで分布が変わった」という一見もっともらしい仮説ではなく、実際にヒストグラムを出してみると**分布は単峰で、閾値の決め方そのものが間違っていた**と分かった。毎回、動き量の分布・閾値・各区間の中央値をダンプしてから直すこと

## 7. ファイル

- `michi-annai-editor.html` … **配布物**。このファイル1つだけで動作する（ビルド不要・サーバー不要・外部依存はGoogle Fontsのみ）
- `sample/` … 動作確認用の実機動画（リポジトリには含めない想定）
- `tools/` … **開発用のみ。配布物には含まれない**
  - `verify-real-video.mjs` … 実機動画で解析を通して結果をJSONで出すスクリプト（6章参照）
  - `ui-check.mjs` … 編集UIを実際にクリック・ドラッグして動作を確認し、スクリーンショットを保存するスクリプト（6章参照）
  - `responsive-check.mjs` … 複数の画面サイズで描画して、はみ出し・タップ目標の大きさを測り、スクリーンショットを保存するスクリプト（6章参照）
  - `export-bar-check.mjs` … 書き出し完了後の固定バーを各画面幅で測るスクリプト（6章参照）
  - `export-format-check.mjs` … 書き出し形式（mp4優先）と共有APIの判定を確認するスクリプト（6章参照）
  - `package.json` / `package-lock.json` / `node_modules/` … 上記が使う `puppeteer-core`

配布時に渡すのは `michi-annai-editor.html` 1ファイルだけでよい。`tools/` はあくまで開発中に「実機で本当に正しく検出できているか」を確認するためのもので、これが無くてもツール自体は動く。
